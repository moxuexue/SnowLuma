{
  "name": "@snowluma/runtime",
  "version": "1.8.0",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "description": "SnowLuma runtime distribution manifest: ships into dist/ (launcher.bat, package.json, native/*.dll, native/*.node). Native libraries are loaded via process.dlopen()."
}
